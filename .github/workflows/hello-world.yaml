name: Java CI
on: push
jobs:
  Clone-down:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    steps:
      - uses: actions/checkout@v2
      - name: Upload Repo
        uses: actions/upload-artifact@v2
        with:
          name: code
          path: .
  Build:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    needs: Clone-down
    steps:
      - name: Downlaod Repo
        uses: actions/download-artifact@v2
        with:
          name: code
          path: .
      - name: my-step
        run: chmod +x ci/build-app.sh && ci/build-app.sh
      - name: Upload new build 
        uses: actions/upload-artifact@v2 #Here
        with:
          name: code
          path: .
  Test:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    needs: Clone-down
    steps:
      - name: Downlaod Repo
        uses: actions/download-artifact@v2
        with:
          name: code
          path: .
      - name: my-test
        run: chmod +x ci/unit-test-app.sh && ci/unit-test-app.sh
  Docker-image:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    needs:
      - Build
      - Test
    steps:
      - name: Download new build 
        uses: actions/upload-artifact@v2 #here
        with:
          name: code
          path: .
      - name: build and push 
        run: ci/build-docker.sh && ci/push-docker.sh
        with: export GIT_COMMIT="GA-$GITHUB_SHA"
    env: 
        docker_username: ${{ secrets.DOCKER_USERNAME }}
        docker_password: 26205667-572f-4e1b-84e6-1a07efc19f83
 # my-job:
 #   runs-on: ubuntu-latest
 #   container: gradle:6-jdk11
 #   steps:
 #     - uses: actions/checkout@v2
 #     - name: my-step
 #       run: ci/build-app.sh
 #     - name: my-test
 #       run: ci/unit-test-app.sh
 #     - name: Upload Repo
 #       uses: actions/upload-artifact@v2
 #       with:
 #         name: code
 #         path: .